abs() ==> for absolute value
math.floor() 
len() ==> to get length
mp = {} # to create the dictionary
min() # to find the min b/w two or more values
min_val = float('-inf')
max_val = float('inf') # initializing
enumerate(arr , start = 1) # for index and val
range(arr.len) # for i = 0 -> arrlength /(random val)
for i in string # directly iterate over the values
^ ==> XOR 
& == ANd
| ==> OR
~ == NOT